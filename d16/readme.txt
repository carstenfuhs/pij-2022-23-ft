Recall:
To compile code in a *package*, you need to call the compiler from
the "root" of the package structure (here: from inside folder d16).

  javac uk/ac/bbk/dcs/pij/*.java

To run a program, write something like

  java uk.ac.bbk.dcs.pij.TextLoop 1

(also from inside folder d16).
